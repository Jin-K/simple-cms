/**
 * The main SelectionToggleItemAction enum
 *
 * @export
 * @enum {number}
 */
export enum SelectionToggleItemAction {
  AddSelected,
  RemoveSelected,
  AddUnselected,
  RemoveUnselected
}

/**
 * The main SelectionIdsStrategy enum
 *
 * @export
 * @enum {number}
 */
export enum SelectionIdsStrategy {
  Normal,
  Inversed
}


/**
 * The main SelectionCode enum
 *
 * @description Check selection-states.md where each code has an example
 * @export
 * @enum {number}
 */
export enum SelectionCode {
  A1,
  A2,
  A3,
  B1,
  B2,
  B3,
  C1,
  C2,
  C3
}
