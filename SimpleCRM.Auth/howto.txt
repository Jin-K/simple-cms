# Build docker image with following command
docker build -t simple-crm/auth .

# Start docker container with following command
docker run -it -p 44321:44321 simple-crm/auth

# delete docker image with following command
docker rmi -f simple-crm/auth

TODO: Checker s'il faut vraiment utiliser dans le dockerfile un self-signed certificate vu que dans le code de Startup.cs il y a une histoire de developer credentials
TODO: Checker si tempkey.rsa est encore nécéssaire (si pas, nettoyer .gitignore)